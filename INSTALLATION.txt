POLICEBOT v1.5.1 DEFINITIF

1. Remplacer tout le projet GitHub par ce dossier (garder seulement les valeurs .env dans Render).
2. git add .
3. git commit -m "PoliceBot v1.5.1 definitive onboarding flow"
4. git push
5. Render > Manual Deploy > Clear build cache & deploy.
6. Arrêter toute instance locale du bot et tout ancien service Render utilisant le même token.

LOG OBLIGATOIRE AU DEMARRAGE:
✅ BUILD 1.5.1-definitif

Si ce log n'apparait pas, Render n'exécute pas cette version.

Flux:
/ac = Accepted CV Police seulement.
/pl = Police + Academy, retrait Citizen + Accepted CV, panneau seulement. Aucun badge et aucun changement de pseudo.
Formulaire = badge + Neon + pseudo [badge] Prénom Nom.
