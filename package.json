{
  "name": "police-bot-discord",
  "version": "1.7.4",
  "description": "Bot Discord police - diagnostic REST/Gateway, token normalisé et intents minimaux pour Render.",
  "main": "src/index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/index.js",
    "deploy": "node src/deploy-commands.js",
    "check": "node --check src/index.js && node --check src/database.js && node --check src/deploy-commands.js && for f in src/commands/*.js src/utils/*.js; do node --check \"$f\" || exit 1; done"
  },
  "engines": {
    "node": "22.x"
  },
  "dependencies": {
    "discord.js": "^14.22.1",
    "dotenv": "^16.6.1",
    "pg": "^8.22.0"
  }
}
